# Node.js dependencies
node_modules/

# Package lock files (package.json itself IS tracked - it just declares the
# test script, no dependencies, so there's no lockfile to generate anyway)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Logfiles
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
*.tgz

# Optional: VS Code settings
.vscode/

# Optional: Env files
.env

# Jest Tests
babel.config.js
jest.config.js

dist/
build/
coverage/
